L5:

1. copy catch for http errors from nest js site, in shared : create dir filters: global-exception.filter.ts, add it to app module into providers

2. create Dockerfile, add changes to docker-compose.yml,  docker compose build